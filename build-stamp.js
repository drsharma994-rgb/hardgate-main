/* HARDGATE — build stamp. Single source of truth for "which version am I running?"
   Loaded FIRST so every later script can read G.HG_BUILD. */
(function(){
'use strict';
var G = (typeof window !== 'undefined') ? window : globalThis;

var HG_BUILD = {
  version: 'hg-v953',
  pack: 'GOLD SCALP AND GOLD SWING WERE LISTED COVERED VIA A WEEKEND READ THE PROBED FUNCTIONS DO NOT CONTAIN. hg-v952 made the coverage reporter CALL each route, and two rows were left as bare via strings reading own weekend read. They do read one - in the tab SHELL, in runScan, on the WALL CLOCK. The functions the reporter probes have no calendar at all: goldind.js, the file housing goldScalpSetups, had not ONE weekend reference in the whole file, and goldSwingSetups had none either. That is hg-v952 failure one layer down - a via string naming a read that exists somewhere other than the thing being probed. WHAT IT COST BEYOND THE REPORTER: every consumer of the mint that is not the tab shell got NO weekend awareness - GOLD PINE goldScalpSetups call, the OMNIGOLD bridge, STAR TRADER, and both replay harnesses. The swing walk own artifact meta lists weekend demotes among the runScan stages it does NOT replay, and both walks run on PAXGUSDT, whose universe line in the same meta reads 24/7 weekend bars a broker never printed. MEASURED, and it is a quarter of the biggest gold evidence base in the repo: 679 of GOLD SCALP 2,575 committed trades (26.4%) formed inside the gold weekend, and that walk is what HG_GOLD_SETUP_EDGE - the table deciding suppress, demote and prefer on that desk - is measured on. The split moves the reading in BOTH directions and no verdict is claimed from it: on the tradeable subset the desk reads 38.6% win against the 40.0% its own 1.5R ladder demands, while the whole-book figure it quotes reads exactly 40.0% - at breakeven only because a quarter of it is bars gold never printed. GOLD SWING is 24 of 277 (8.7%) and there the split runs the OTHER way, so there is no one-directional story and none is offered. Re-derive: node scripts/gold-weekend-population.mjs, which CALLS the repo own DST-aware calendar and contains no calendar arithmetic of its own. THE FIX PUTS THE CALENDAR WHERE THE MINT IS, keyed on the SIGNAL BAR - both mints already take inp.now and both replay harnesses already pass the closed bar cutoff there, so a Monday re-run over Friday bars gives Friday the answer with no new plumbing, proved by driving the real mint under two opposing wall clocks. It MARKS AND WITHHOLDS NOTHING, deliberately and for two reasons: the shell already demotes on the live path and a second withhold would move the board on evidence this pack did not gather; and the replay rows need the mark precisely so the population can be SEPARATED later, which is exactly how NEW GOLD record came to be half weekend unnoticed. The rejected side-channel is marked too, because that is the channel the replay harness settles WITH the plan. A SIXTH INSTANCE OF THE null-coerces-to-zero TRAP, in the shared calendar itself: empty string and zero both coerce to 0, which is 1970-01-01, a THURSDAY - so an unreadable instant read as gold was open and marked a candidate accordingly, silently. An instant at or before the epoch is a failed read, not a cheerful answer, and now yields no verdict. TWO SURVIVORS THE MUTATION PASS FOUND IN MY OWN GUARD, both the same blind spot: asserting on rows that already carry the value. Every candidate this desk forms is ALREADY demoted, so a mutation that ALSO set demoted was invisible to a board comparison; and best is a member of ranked, so removing the branch that marks it changed nothing by reference. The shared helper is now driven DIRECTLY on pristine objects, where the assertion is that marking adds exactly goldShut and goldShutWhy and nothing else - no demote, no veto, no stamp. Both desks now report VERIFIED rather than sitting in the author-claim bucket. Fails OPEN at every seam. No gate, threshold or verdict moves on any desk, and no setup leaves any board. Tests: tests/test-gold-mint-weekend.mjs, 56 assertions, 19 of 19 behavioural mutations caught across gold-formation.js, goldind.js and goldswing.js with the baseline verified green first',
  built: '2026-09-24T00:00:00Z'
};

function hgBuildLabel(b){
  b = b || HG_BUILD;
  var v = String(b.version || '').replace(/^hg-/, '');
  var p = b.pack ? ' · ' + b.pack : '';
  return v ? v + p : 'unknown build';
}

function hgBuildParseVersion(text){
  try{
    if (typeof text !== 'string' || !text) return null;
    var m = text.match(/version\s*:\s*['"]([A-Za-z0-9._-]{1,64})['"]/);
    return m ? m[1] : null;
  }catch(e){ return null; }
}

function hgBuildCompare(loaded, live){
  if (!loaded || !live) return { state: 'unknown', reason: 'could not read one side' };
  if (String(loaded) === String(live)) return { state: 'fresh', reason: 'matches server' };
  return { state: 'stale', reason: 'server is on ' + live + ', this tab loaded ' + loaded };
}

function hgBuildDistance(loaded, live){
  try{
    var a = String(loaded || '').match(/(\d+)\s*$/);
    var b = String(live || '').match(/(\d+)\s*$/);
    if (!a || !b) return null;
    return (+b[1]) - (+a[1]);
  }catch(e){ return null; }
}

function hgBuildChipState(res, b){
  b = b || HG_BUILD;
  var label = hgBuildLabel(b);
  if (!res || res.state === 'pending') return { text: label, cls: 'ok', title: 'checking for newer build' };
  if (res.state === 'fresh') return { text: label, cls: 'ok', title: 'running current build (' + res.live + ')' };
  if (res.state === 'stale'){
    var d = hgBuildDistance(res.loaded, res.live);
    var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
    return { text: label + ' · STALE' + behind, cls: 'bad', title: res.reason + ' — hard-reload to pick it up' };
  }
  return { text: label + ' · ?', cls: 'warn', title: (res.reason || 'freshness unknown') + ' — offline or blocked' };
}

function hgBuildFreshness(fetchImpl){
  var f = (typeof fetchImpl === 'function') ? fetchImpl : (typeof G.fetch === 'function' ? G.fetch.bind(G) : null);
  var loaded = HG_BUILD.version;
  if (!f) return Promise.resolve({ state: 'unknown', loaded: loaded, live: null, reason: 'no fetch' });
  var url = './build-stamp.js?fresh=' + Date.now();
  return f(url, { cache: 'no-store' }).then(function(res){
    if (!res || !res.ok) throw new Error('http ' + (res && res.status));
    return res.text();
  }).then(function(text){
    var live = hgBuildParseVersion(text);
    var cmp = hgBuildCompare(loaded, live);
    return { state: cmp.state, loaded: loaded, live: live, reason: cmp.reason };
  }).catch(function(e){
    return { state: 'unknown', loaded: loaded, live: null, reason: (e && e.message) || 'network failed' };
  });
}

function hgBuildSwWaiting(){
  try{
    var sw = G.navigator && G.navigator.serviceWorker;
    if (!sw || !sw.controller) return Promise.resolve(false);
    return sw.getRegistration().then(function(reg){
      return !!(reg && reg.waiting);
    }).catch(function(){ return false; });
  }catch(e){ return Promise.resolve(false); }
}

function hgRenderBuildChip(el, res){
  try{
    var node = el || (G.document && G.document.getElementById('chipBuild'));
    if (!node) return null;
    var st = hgBuildChipState(res);
    node.textContent = '';
    var b = G.document.createElement('b');
    b.textContent = st.text;
    node.appendChild(b);
    node.title = st.title;
    node.className = 'statuschip hg-build-' + st.cls;
    return st;
  }catch(e){ return null; }
}

function hgRenderVerBadge(res){
  try{
    var node = G.document && G.document.getElementById('hgVerBadge');
    if (!node) return null;
    var v = String(HG_BUILD.version || '').replace(/^hg-/, '');
    if (!v){ node.style.display = 'none'; return null; }
    if (res && res.state === 'stale'){
      var d = hgBuildDistance(res.loaded, res.live);
      var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
      node.textContent = v + ' · STALE' + behind;
      node.className = 'verbadge verbadge-stale';
      node.title = (res.reason || 'stale') + ' — reloading…';
      return { stale: true };
    }
    node.textContent = v;
    node.className = 'verbadge';
    node.title = HG_BUILD.version + (HG_BUILD.pack ? (' · ' + HG_BUILD.pack) : '');
    return { stale: false };
  }catch(e){ return null; }
}

function hgBuildMaybeReload(res, storage, reloadFn){
  try{
    if (!res || res.state !== 'stale' || !res.live) return false;
    var store = storage;
    if (!store && G.sessionStorage) store = G.sessionStorage;
    if (!store || typeof store.getItem !== 'function') return false;
    var key = 'hg_build_reload_' + String(res.live);
    if (store.getItem(key)) return false;
    var reload = reloadFn;
    if (!reload && G.location && typeof G.location.reload === 'function') reload = G.location.reload.bind(G.location);
    if (typeof reload !== 'function') return false;
    store.setItem(key, '1');
    reload();
    return true;
  }catch(e){}
  return false;
}

function hgBuildApplyFreshness(res){
  try{
    if (!res) return res;
    hgRenderBuildChip(null, res);
    hgRenderVerBadge(res);
    G.HG_BUILD_FRESHNESS = res;
    hgBuildMaybeReload(res);
    return res;
  }catch(e){ return res; }
}

function hgBuildInit(){
  try{
    hgRenderBuildChip(null, null);
    hgRenderVerBadge(null);
    hgBuildFreshness().then(function(res){
      return hgBuildSwWaiting().then(function(waiting){
        if (waiting && res.state === 'fresh'){
          res = { state: 'stale', loaded: res.loaded, live: res.live, reason: 'new build downloaded' };
        }
        return hgBuildApplyFreshness(res);
      });
    });
    if (G.document && typeof G.document.addEventListener === 'function'){
      G.document.addEventListener('visibilitychange', function(){
        try{
          if (!G.document || G.document.visibilityState !== 'visible') return;
          hgBuildFreshness().then(hgBuildApplyFreshness);
        }catch(e){}
      });
    }
  }catch(e){}
}

G.HG_BUILD = HG_BUILD;
G.hgBuildLabel = hgBuildLabel;
G.hgBuildParseVersion = hgBuildParseVersion;
G.hgBuildCompare = hgBuildCompare;
G.hgBuildDistance = hgBuildDistance;
G.hgBuildChipState = hgBuildChipState;
G.hgBuildFreshness = hgBuildFreshness;
G.hgBuildSwWaiting = hgBuildSwWaiting;
G.hgRenderBuildChip = hgRenderBuildChip;
G.hgRenderVerBadge = hgRenderVerBadge;
G.hgBuildMaybeReload = hgBuildMaybeReload;
G.hgBuildApplyFreshness = hgBuildApplyFreshness;
G.hgBuildInit = hgBuildInit;

try{
  if (G.document){
    if (G.document.readyState === 'loading'){
      G.document.addEventListener('DOMContentLoaded', hgBuildInit);
    } else {
      hgBuildInit();
    }
  }
}catch(e){}

})();
