/* HARDGATE — build stamp. Single source of truth for "which version am I running?"
   Loaded FIRST so every later script can read G.HG_BUILD. */
(function(){
'use strict';
var G = (typeof window !== 'undefined') ? window : globalThis;

var HG_BUILD = {
  version: 'hg-v950',
  pack: 'A SESSION RULE THAT ADMITS THE WEEKEND, AND THE FOUR DESKS hg-v949 COULD NOT REACH. v949 wired gold trading calendar into the shared formation, and its own coverage reporter named who was left: OPTI GOLD, GOLD PINE, GOLD PRO and 80PERCENT mint from RAW BARS and never route through it. This pack covers all four. THE HEADLINE IS NOT A MISSING RULE, IT IS A FALSE PREMISE. 80PERCENT HAS a session rule, hg80InSession, and it reads the UTC HOUR ONLY and never the day, so a Saturday 14:00 bar passes its 13:00-18:00 NY session test. Measured with the repo own calendar: 40 of the 672 hours in a 28-day span pass that test WHILE GOLD IS SHUT. THAT WAS A DELIBERATE, DOCUMENTED CHOICE, and half of it still stands. The file said adding a weekday condition would be a silent deviation from the supplied spec - true, and this pack does not touch the firing rule. It also said it would change nothing on real data BECAUSE GOLD PRINTS NO WEEKEND BARS - and that is false for the feed this desk actually reads. Bars arrive through hgOgFetchRows, whose chain ends in binanceKlines of PAXGUSDT, a 24/7 Binance pair, and that is the leg which fires whenever XM is not configured, the default. NEW GOLD committed walk is the proof it is not hypothetical: 9 of its 19 trades formed inside the gold weekend on PAXGUSDT. So the SPEC decides what fires and the CALENDAR decides what the desk PRESENTS AS TRADEABLE. EACH DESK IS STAMPED AT THE INSTANT ITS OWN SHAPE MAKES CORRECT, and the four differ, which is why one copied shape would have been wrong on three of them. OPTI GOLD walks 400 to 600 bars and emits breaks from across that window, so it stamps PER SETUP from the setup own break bar - the guard proves a single scan carries both shut and open setups, which a per-scan stamp could not do - and a shut-market break loses its ACTIONABLE claim while keeping its ENTRY, STOP and T1. 80PERCENT walks bars too and stamps PER SIGNAL. GOLD PINE setups carry no instant at all, so it stamps PER MODE from each mode own series, 4h for swing and 15m for scalp, because one instant for both would mislabel one of them. GOLD PRO composes one plan from the last closed bar of the series in hand, so a single instant is right there. EVERYTHING FAILS OPEN: no calendar, an unreadable instant or an empty series and the verdict is null and nothing is withheld, proved on every one of the four. Nothing is deleted anywhere - levels, records and firing all stand. THE COVERAGE REPORTER NOW ROUTES ALL FOUR, each with its route NAMED rather than a bare covered flag, and because every desk on the list now has a route the uncovered bucket would be unreachable - so the guard pushes a synthetic routeless desk and requires it to still report UNCOVERED, because a bucket nothing can land in is a rubber stamp. ONE VACUITY CAUGHT BY MUTATION, AGAIN: the first cut of the 80PERCENT section ran a random tape that fired ZERO signals and passed through its own else branch, and three mutations survived. The tape is now built to fire - a trend with periodic dips through the RSI threshold, spanning the Friday close - and the guard asserts the fire count is non-zero FIRST, so a future zero fails loudly instead of passing quietly. A second fixture bug was found the same way: sections 6 and 7 anchored the tape START on a Saturday, putting the last bar thirteen days later on a weekday. No gate, threshold or verdict moves on any other desk, and no setup leaves any board. Tests: tests/test-gold-weekend-minters.mjs, 39 assertions, 16 of 16 behavioural mutations caught across five files with the baseline verified green first',
  built: '2026-09-24T00:00:00Z'
};

function hgBuildLabel(b){
  b = b || HG_BUILD;
  var v = String(b.version || '').replace(/^hg-/, '');
  var p = b.pack ? ' · ' + b.pack : '';
  return v ? v + p : 'unknown build';
}

function hgBuildParseVersion(text){
  try{
    if (typeof text !== 'string' || !text) return null;
    var m = text.match(/version\s*:\s*['"]([A-Za-z0-9._-]{1,64})['"]/);
    return m ? m[1] : null;
  }catch(e){ return null; }
}

function hgBuildCompare(loaded, live){
  if (!loaded || !live) return { state: 'unknown', reason: 'could not read one side' };
  if (String(loaded) === String(live)) return { state: 'fresh', reason: 'matches server' };
  return { state: 'stale', reason: 'server is on ' + live + ', this tab loaded ' + loaded };
}

function hgBuildDistance(loaded, live){
  try{
    var a = String(loaded || '').match(/(\d+)\s*$/);
    var b = String(live || '').match(/(\d+)\s*$/);
    if (!a || !b) return null;
    return (+b[1]) - (+a[1]);
  }catch(e){ return null; }
}

function hgBuildChipState(res, b){
  b = b || HG_BUILD;
  var label = hgBuildLabel(b);
  if (!res || res.state === 'pending') return { text: label, cls: 'ok', title: 'checking for newer build' };
  if (res.state === 'fresh') return { text: label, cls: 'ok', title: 'running current build (' + res.live + ')' };
  if (res.state === 'stale'){
    var d = hgBuildDistance(res.loaded, res.live);
    var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
    return { text: label + ' · STALE' + behind, cls: 'bad', title: res.reason + ' — hard-reload to pick it up' };
  }
  return { text: label + ' · ?', cls: 'warn', title: (res.reason || 'freshness unknown') + ' — offline or blocked' };
}

function hgBuildFreshness(fetchImpl){
  var f = (typeof fetchImpl === 'function') ? fetchImpl : (typeof G.fetch === 'function' ? G.fetch.bind(G) : null);
  var loaded = HG_BUILD.version;
  if (!f) return Promise.resolve({ state: 'unknown', loaded: loaded, live: null, reason: 'no fetch' });
  var url = './build-stamp.js?fresh=' + Date.now();
  return f(url, { cache: 'no-store' }).then(function(res){
    if (!res || !res.ok) throw new Error('http ' + (res && res.status));
    return res.text();
  }).then(function(text){
    var live = hgBuildParseVersion(text);
    var cmp = hgBuildCompare(loaded, live);
    return { state: cmp.state, loaded: loaded, live: live, reason: cmp.reason };
  }).catch(function(e){
    return { state: 'unknown', loaded: loaded, live: null, reason: (e && e.message) || 'network failed' };
  });
}

function hgBuildSwWaiting(){
  try{
    var sw = G.navigator && G.navigator.serviceWorker;
    if (!sw || !sw.controller) return Promise.resolve(false);
    return sw.getRegistration().then(function(reg){
      return !!(reg && reg.waiting);
    }).catch(function(){ return false; });
  }catch(e){ return Promise.resolve(false); }
}

function hgRenderBuildChip(el, res){
  try{
    var node = el || (G.document && G.document.getElementById('chipBuild'));
    if (!node) return null;
    var st = hgBuildChipState(res);
    node.textContent = '';
    var b = G.document.createElement('b');
    b.textContent = st.text;
    node.appendChild(b);
    node.title = st.title;
    node.className = 'statuschip hg-build-' + st.cls;
    return st;
  }catch(e){ return null; }
}

function hgRenderVerBadge(res){
  try{
    var node = G.document && G.document.getElementById('hgVerBadge');
    if (!node) return null;
    var v = String(HG_BUILD.version || '').replace(/^hg-/, '');
    if (!v){ node.style.display = 'none'; return null; }
    if (res && res.state === 'stale'){
      var d = hgBuildDistance(res.loaded, res.live);
      var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
      node.textContent = v + ' · STALE' + behind;
      node.className = 'verbadge verbadge-stale';
      node.title = (res.reason || 'stale') + ' — reloading…';
      return { stale: true };
    }
    node.textContent = v;
    node.className = 'verbadge';
    node.title = HG_BUILD.version + (HG_BUILD.pack ? (' · ' + HG_BUILD.pack) : '');
    return { stale: false };
  }catch(e){ return null; }
}

function hgBuildMaybeReload(res, storage, reloadFn){
  try{
    if (!res || res.state !== 'stale' || !res.live) return false;
    var store = storage;
    if (!store && G.sessionStorage) store = G.sessionStorage;
    if (!store || typeof store.getItem !== 'function') return false;
    var key = 'hg_build_reload_' + String(res.live);
    if (store.getItem(key)) return false;
    var reload = reloadFn;
    if (!reload && G.location && typeof G.location.reload === 'function') reload = G.location.reload.bind(G.location);
    if (typeof reload !== 'function') return false;
    store.setItem(key, '1');
    reload();
    return true;
  }catch(e){}
  return false;
}

function hgBuildApplyFreshness(res){
  try{
    if (!res) return res;
    hgRenderBuildChip(null, res);
    hgRenderVerBadge(res);
    G.HG_BUILD_FRESHNESS = res;
    hgBuildMaybeReload(res);
    return res;
  }catch(e){ return res; }
}

function hgBuildInit(){
  try{
    hgRenderBuildChip(null, null);
    hgRenderVerBadge(null);
    hgBuildFreshness().then(function(res){
      return hgBuildSwWaiting().then(function(waiting){
        if (waiting && res.state === 'fresh'){
          res = { state: 'stale', loaded: res.loaded, live: res.live, reason: 'new build downloaded' };
        }
        return hgBuildApplyFreshness(res);
      });
    });
    if (G.document && typeof G.document.addEventListener === 'function'){
      G.document.addEventListener('visibilitychange', function(){
        try{
          if (!G.document || G.document.visibilityState !== 'visible') return;
          hgBuildFreshness().then(hgBuildApplyFreshness);
        }catch(e){}
      });
    }
  }catch(e){}
}

G.HG_BUILD = HG_BUILD;
G.hgBuildLabel = hgBuildLabel;
G.hgBuildParseVersion = hgBuildParseVersion;
G.hgBuildCompare = hgBuildCompare;
G.hgBuildDistance = hgBuildDistance;
G.hgBuildChipState = hgBuildChipState;
G.hgBuildFreshness = hgBuildFreshness;
G.hgBuildSwWaiting = hgBuildSwWaiting;
G.hgRenderBuildChip = hgRenderBuildChip;
G.hgRenderVerBadge = hgRenderVerBadge;
G.hgBuildMaybeReload = hgBuildMaybeReload;
G.hgBuildApplyFreshness = hgBuildApplyFreshness;
G.hgBuildInit = hgBuildInit;

try{
  if (G.document){
    if (G.document.readyState === 'loading'){
      G.document.addEventListener('DOMContentLoaded', hgBuildInit);
    } else {
      hgBuildInit();
    }
  }
}catch(e){}

})();
