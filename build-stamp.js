/* HARDGATE — build stamp. Single source of truth for "which version am I running?"
   Loaded FIRST so every later script can read G.HG_BUILD. */
(function(){
'use strict';
var G = (typeof window !== 'undefined') ? window : globalThis;

var HG_BUILD = {
  version: 'hg-v954',
  pack: 'THE COVERAGE REPORTER DESK LIST WAS ITSELF HAND-TYPED, WHICH IS THE MECHANISM THAT PRODUCED ITS TWO PREVIOUS BLIND SPOTS. hg-v951 added the inline GOLD tab to that list by noticing. hg-v952 added TAURIC by noticing. Each was written up as a blind spot fixed, and the thing that produced both -- a desk list kept by hand, inside the reporter built so that gaps name themselves -- was never touched. The shell GOLD nav group holds SEVENTEEN tabs and the reporter listed TWELVE. The five missing: MILLI GOLD, GOLD DIRECTION, GOLD ULTRA, GOLD SPOT, GOLD COINT. TWO OF THEM WRITE ticket:true XAUUSD ROWS INTO THE FORWARD LEDGER with no weekend reference of any kind. That is stronger than the TAURIC case hg-v952 wired: those rows record ticket:false, so there was no ticket to withhold. These record TICKETS, so a weekend-formed pick entered the ledger as tradeable and would be judged as one -- the contamination hg-v949 measured on NEW GOLD, now running in two more ledgers. GOLD ULTRA only two mentions of the word weekend are PROSE IN ITS WALK METADATA, saying its universe carries 24/7 weekend bars a broker never printed: the desk documented the contamination and did nothing about it. Both MARK and withhold nothing, on the TAURIC precedent, keyed to their own signal bar (1h for GOLD DIRECTION, 15m for GOLD ULTRA) rather than the wall clock -- proved by driving each under a wall clock on the opposite side of the calendar. THE OTHER THREE ARE CLASSIFIED RATHER THAN WIRED, and the distinction is the honest part: MILLI GOLD INHERITS OMNIGOLD, because since hg-v938 it renders the cards OMNIGOLD has already evaluated rather than scanning, so a second calendar here would be a second calendar; GOLD SPOT is a spot-perp basis monitor and GOLD COINT says in its own header that it is CONTEXT only with no spread execution path. That last claim is ASSERTED BEHAVIOURALLY, not assumed: each is booted, refreshed, and required to write no forward record at all. THE CENSUS CANNOT GO STALE SILENTLY AGAIN. Every gold tab is now classified as a minter or a non-minter, the guard reads HG_NAV_GROUPS out of index.html by EXECUTING the declaration rather than grepping it (the hg-v951 lesson, after four brittle source-slice guards this session), and requires the two lists to agree in both directions -- no gold tab unclassified, nothing classified that the shell does not list, and no tab in both buckets. hgGoldWeekendCensusGaps reports it at runtime, and an unreadable registry returns NULL rather than an empty gap list, because an unreadable census is not a clean one. The guard proves the reporter is not a rubber stamp by pushing a synthetic gold tab and requiring it to be named. A DEFECT THIS PACK OWN GUARD FOUND IN THIS PACK OWN WIRING: the coverage probe wants truthy-when-shut and null-when-open, so a route that cannot tell two known instants apart reads BROKEN -- but the MARK needs three states, shut, open and UNREADABLE, and the probe shape makes the last two identical. Both desks were written with the probe shape, so every Wednesday row silently carried no mark at all. The full verdict is read for the mark and wrapped for the probe. FOUR MUTATIONS SURVIVED THE FIRST GOLD ULTRA PASS, all one gap: the guard exercised its exported helpers and never its REAL record path, which is the hg-v952 TAURIC failure exactly. The shipped statement is now LIFTED OUT OF THE FILE AND EXECUTED under stubs inside the module own context, so what is driven is the shipped code rather than a restatement of it, and deleting the mark outright is caught. No gate, threshold or verdict moves on any desk, and no setup leaves any board. Tests: tests/test-gold-census.mjs, 77 assertions, 25 of 25 behavioural mutations caught across gold-formation.js, golddirection.js and goldultra.js with the baseline verified green before every pass',
  built: '2026-09-24T00:00:00Z'
};

function hgBuildLabel(b){
  b = b || HG_BUILD;
  var v = String(b.version || '').replace(/^hg-/, '');
  var p = b.pack ? ' · ' + b.pack : '';
  return v ? v + p : 'unknown build';
}

function hgBuildParseVersion(text){
  try{
    if (typeof text !== 'string' || !text) return null;
    var m = text.match(/version\s*:\s*['"]([A-Za-z0-9._-]{1,64})['"]/);
    return m ? m[1] : null;
  }catch(e){ return null; }
}

function hgBuildCompare(loaded, live){
  if (!loaded || !live) return { state: 'unknown', reason: 'could not read one side' };
  if (String(loaded) === String(live)) return { state: 'fresh', reason: 'matches server' };
  return { state: 'stale', reason: 'server is on ' + live + ', this tab loaded ' + loaded };
}

function hgBuildDistance(loaded, live){
  try{
    var a = String(loaded || '').match(/(\d+)\s*$/);
    var b = String(live || '').match(/(\d+)\s*$/);
    if (!a || !b) return null;
    return (+b[1]) - (+a[1]);
  }catch(e){ return null; }
}

function hgBuildChipState(res, b){
  b = b || HG_BUILD;
  var label = hgBuildLabel(b);
  if (!res || res.state === 'pending') return { text: label, cls: 'ok', title: 'checking for newer build' };
  if (res.state === 'fresh') return { text: label, cls: 'ok', title: 'running current build (' + res.live + ')' };
  if (res.state === 'stale'){
    var d = hgBuildDistance(res.loaded, res.live);
    var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
    return { text: label + ' · STALE' + behind, cls: 'bad', title: res.reason + ' — hard-reload to pick it up' };
  }
  return { text: label + ' · ?', cls: 'warn', title: (res.reason || 'freshness unknown') + ' — offline or blocked' };
}

function hgBuildFreshness(fetchImpl){
  var f = (typeof fetchImpl === 'function') ? fetchImpl : (typeof G.fetch === 'function' ? G.fetch.bind(G) : null);
  var loaded = HG_BUILD.version;
  if (!f) return Promise.resolve({ state: 'unknown', loaded: loaded, live: null, reason: 'no fetch' });
  var url = './build-stamp.js?fresh=' + Date.now();
  return f(url, { cache: 'no-store' }).then(function(res){
    if (!res || !res.ok) throw new Error('http ' + (res && res.status));
    return res.text();
  }).then(function(text){
    var live = hgBuildParseVersion(text);
    var cmp = hgBuildCompare(loaded, live);
    return { state: cmp.state, loaded: loaded, live: live, reason: cmp.reason };
  }).catch(function(e){
    return { state: 'unknown', loaded: loaded, live: null, reason: (e && e.message) || 'network failed' };
  });
}

function hgBuildSwWaiting(){
  try{
    var sw = G.navigator && G.navigator.serviceWorker;
    if (!sw || !sw.controller) return Promise.resolve(false);
    return sw.getRegistration().then(function(reg){
      return !!(reg && reg.waiting);
    }).catch(function(){ return false; });
  }catch(e){ return Promise.resolve(false); }
}

function hgRenderBuildChip(el, res){
  try{
    var node = el || (G.document && G.document.getElementById('chipBuild'));
    if (!node) return null;
    var st = hgBuildChipState(res);
    node.textContent = '';
    var b = G.document.createElement('b');
    b.textContent = st.text;
    node.appendChild(b);
    node.title = st.title;
    node.className = 'statuschip hg-build-' + st.cls;
    return st;
  }catch(e){ return null; }
}

function hgRenderVerBadge(res){
  try{
    var node = G.document && G.document.getElementById('hgVerBadge');
    if (!node) return null;
    var v = String(HG_BUILD.version || '').replace(/^hg-/, '');
    if (!v){ node.style.display = 'none'; return null; }
    if (res && res.state === 'stale'){
      var d = hgBuildDistance(res.loaded, res.live);
      var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
      node.textContent = v + ' · STALE' + behind;
      node.className = 'verbadge verbadge-stale';
      node.title = (res.reason || 'stale') + ' — reloading…';
      return { stale: true };
    }
    node.textContent = v;
    node.className = 'verbadge';
    node.title = HG_BUILD.version + (HG_BUILD.pack ? (' · ' + HG_BUILD.pack) : '');
    return { stale: false };
  }catch(e){ return null; }
}

function hgBuildMaybeReload(res, storage, reloadFn){
  try{
    if (!res || res.state !== 'stale' || !res.live) return false;
    var store = storage;
    if (!store && G.sessionStorage) store = G.sessionStorage;
    if (!store || typeof store.getItem !== 'function') return false;
    var key = 'hg_build_reload_' + String(res.live);
    if (store.getItem(key)) return false;
    var reload = reloadFn;
    if (!reload && G.location && typeof G.location.reload === 'function') reload = G.location.reload.bind(G.location);
    if (typeof reload !== 'function') return false;
    store.setItem(key, '1');
    reload();
    return true;
  }catch(e){}
  return false;
}

function hgBuildApplyFreshness(res){
  try{
    if (!res) return res;
    hgRenderBuildChip(null, res);
    hgRenderVerBadge(res);
    G.HG_BUILD_FRESHNESS = res;
    hgBuildMaybeReload(res);
    return res;
  }catch(e){ return res; }
}

function hgBuildInit(){
  try{
    hgRenderBuildChip(null, null);
    hgRenderVerBadge(null);
    hgBuildFreshness().then(function(res){
      return hgBuildSwWaiting().then(function(waiting){
        if (waiting && res.state === 'fresh'){
          res = { state: 'stale', loaded: res.loaded, live: res.live, reason: 'new build downloaded' };
        }
        return hgBuildApplyFreshness(res);
      });
    });
    if (G.document && typeof G.document.addEventListener === 'function'){
      G.document.addEventListener('visibilitychange', function(){
        try{
          if (!G.document || G.document.visibilityState !== 'visible') return;
          hgBuildFreshness().then(hgBuildApplyFreshness);
        }catch(e){}
      });
    }
  }catch(e){}
}

G.HG_BUILD = HG_BUILD;
G.hgBuildLabel = hgBuildLabel;
G.hgBuildParseVersion = hgBuildParseVersion;
G.hgBuildCompare = hgBuildCompare;
G.hgBuildDistance = hgBuildDistance;
G.hgBuildChipState = hgBuildChipState;
G.hgBuildFreshness = hgBuildFreshness;
G.hgBuildSwWaiting = hgBuildSwWaiting;
G.hgRenderBuildChip = hgRenderBuildChip;
G.hgRenderVerBadge = hgRenderVerBadge;
G.hgBuildMaybeReload = hgBuildMaybeReload;
G.hgBuildApplyFreshness = hgBuildApplyFreshness;
G.hgBuildInit = hgBuildInit;

try{
  if (G.document){
    if (G.document.readyState === 'loading'){
      G.document.addEventListener('DOMContentLoaded', hgBuildInit);
    } else {
      hgBuildInit();
    }
  }
}catch(e){}

})();
