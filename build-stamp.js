/* HARDGATE — build stamp. Single source of truth for "which version am I running?"
   Loaded FIRST so every later script can read G.HG_BUILD. */
(function(){
'use strict';
var G = (typeof window !== 'undefined') ? window : globalThis;

var HG_BUILD = {
  version: 'hg-v936',
  pack: 'MILLI GOLD - A TAB THAT FORMS ONLY FROM THE MECHANICS THAT MEASURED PROFITABLE, AND SAYS EXACTLY WHAT THAT IS WORTH. Asked for: gold setups built from only the indicators and strategies that have paid across the gold desks. That is a derivable question and the roster is derived, not chosen: a mechanic is admitted when its GATE-CLEAR record - the population that cleared OMNIGOLDs stack, which hg-v917 showed is the honest line and is worse than the unscoped one for 42 of 54 mechanics - is net-positive at XM on at least 20 firings. NINE OF 54 QUALIFY: P5-DRIVE, STRUCT-BOS, SQUEEZE-FIRE, CUSUM-SHIFT, MMOVE, TREND-RECLAIM, P6-COMP, EQH-SWEEP, BOS-RETEST. Pooled: 678 settled trades, gross +0.0887R, net +0.0565R at XM. The sample floor is OMNIGOLDs own MIN_SAMPLES and the guard asserts they are the same number, because a roster using a looser floor than the desks evidence gate would admit mechanics that gate calls too thin to judge. IT IS NOT A NEW ENGINE. Every detector, gate, plan and grade is OMNIGOLDs, reached through hgOgFetchRows / hgOgDetect / hgOgEvaluate. A second copy would drift from the first the day either was touched. The one thing this tab changes is WHICH mechanics may reach a card, and the filter runs on the DETECT output - before evaluation - so an off-roster mechanic is never scored, graded or ranked and cannot arrive by another path. With OMNIGOLD absent the tab says so rather than inventing a fallback engine. NOTHING IS LOOSENED: the roster only ever REMOVES, so G1-G7, the cost ceiling, the one-at-a-time hold and the measured-edge veto all still apply, being the same code. AND THE DISCLOSURE LEADS THE TAB RATHER THAN SITTING UNDER IT, because a tab called the profitable ones is exactly the shape a reader over-trusts. hg-v935 tested THIS EXACT SELECTION out of sample - four disjoint windows, the mechanics ranked on the other three each time, both fill bounds - and keeping the net-positive mechanics scored 13-14 OF 16, NEVER UNANIMOUS. So +0.0565R is what these nine did IN THE BOOK THEY WERE CHOSEN FROM: an UPPER BOUND, not a forecast, and the panel says so in those words. Each is also inside the noise alone - the largest cluster-robust t on the roster is 1.55 against a family bar above 3.1 - so this is the best available reading of which mechanics have paid, not a demonstration that any of them pays. hg-v920 is in this repo because a selection that looked this good in sample was worse in three of four disjoint windows. AN EMPTY ROSTER SHOWS NOTHING and never falls back to the full register: only the measured ones with no measurements is a different desk, not a smaller one. Every card carries its mechanics record and calls it IN SAMPLE, so no card reads as a forward claim. A quiet scan says silence is by construction - nine mechanics against a whole register - rather than looking broken. GENERATED, NEVER TYPED: scripts/milli-gold-roster.mjs writes HG_MILLI_ROSTER from the replay (the hg-v921 rule), refuses to write an EMPTY roster, and npm run gold:milli is the read-only drift check. The guard re-derives every figure from the artifact rather than reading it back from the JS, proves the rebuild ON STRINGS without touching disk, and asserts membership follows the rule exactly for all 54 measured mechanics rather than only for the nine admitted. Tests: tests/test-milli-gold.mjs (188 assertions); 7 of 7 mutations caught after a first run left one survivor - the card record chip, a real gap. Suite: 590 files, 0 failed',
  built: '2026-09-23T00:00:00Z'
};

function hgBuildLabel(b){
  b = b || HG_BUILD;
  var v = String(b.version || '').replace(/^hg-/, '');
  var p = b.pack ? ' · ' + b.pack : '';
  return v ? v + p : 'unknown build';
}

function hgBuildParseVersion(text){
  try{
    if (typeof text !== 'string' || !text) return null;
    var m = text.match(/version\s*:\s*['"]([A-Za-z0-9._-]{1,64})['"]/);
    return m ? m[1] : null;
  }catch(e){ return null; }
}

function hgBuildCompare(loaded, live){
  if (!loaded || !live) return { state: 'unknown', reason: 'could not read one side' };
  if (String(loaded) === String(live)) return { state: 'fresh', reason: 'matches server' };
  return { state: 'stale', reason: 'server is on ' + live + ', this tab loaded ' + loaded };
}

function hgBuildDistance(loaded, live){
  try{
    var a = String(loaded || '').match(/(\d+)\s*$/);
    var b = String(live || '').match(/(\d+)\s*$/);
    if (!a || !b) return null;
    return (+b[1]) - (+a[1]);
  }catch(e){ return null; }
}

function hgBuildChipState(res, b){
  b = b || HG_BUILD;
  var label = hgBuildLabel(b);
  if (!res || res.state === 'pending') return { text: label, cls: 'ok', title: 'checking for newer build' };
  if (res.state === 'fresh') return { text: label, cls: 'ok', title: 'running current build (' + res.live + ')' };
  if (res.state === 'stale'){
    var d = hgBuildDistance(res.loaded, res.live);
    var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
    return { text: label + ' · STALE' + behind, cls: 'bad', title: res.reason + ' — hard-reload to pick it up' };
  }
  return { text: label + ' · ?', cls: 'warn', title: (res.reason || 'freshness unknown') + ' — offline or blocked' };
}

function hgBuildFreshness(fetchImpl){
  var f = (typeof fetchImpl === 'function') ? fetchImpl : (typeof G.fetch === 'function' ? G.fetch.bind(G) : null);
  var loaded = HG_BUILD.version;
  if (!f) return Promise.resolve({ state: 'unknown', loaded: loaded, live: null, reason: 'no fetch' });
  var url = './build-stamp.js?fresh=' + Date.now();
  return f(url, { cache: 'no-store' }).then(function(res){
    if (!res || !res.ok) throw new Error('http ' + (res && res.status));
    return res.text();
  }).then(function(text){
    var live = hgBuildParseVersion(text);
    var cmp = hgBuildCompare(loaded, live);
    return { state: cmp.state, loaded: loaded, live: live, reason: cmp.reason };
  }).catch(function(e){
    return { state: 'unknown', loaded: loaded, live: null, reason: (e && e.message) || 'network failed' };
  });
}

function hgBuildSwWaiting(){
  try{
    var sw = G.navigator && G.navigator.serviceWorker;
    if (!sw || !sw.controller) return Promise.resolve(false);
    return sw.getRegistration().then(function(reg){
      return !!(reg && reg.waiting);
    }).catch(function(){ return false; });
  }catch(e){ return Promise.resolve(false); }
}

function hgRenderBuildChip(el, res){
  try{
    var node = el || (G.document && G.document.getElementById('chipBuild'));
    if (!node) return null;
    var st = hgBuildChipState(res);
    node.textContent = '';
    var b = G.document.createElement('b');
    b.textContent = st.text;
    node.appendChild(b);
    node.title = st.title;
    node.className = 'statuschip hg-build-' + st.cls;
    return st;
  }catch(e){ return null; }
}

function hgRenderVerBadge(res){
  try{
    var node = G.document && G.document.getElementById('hgVerBadge');
    if (!node) return null;
    var v = String(HG_BUILD.version || '').replace(/^hg-/, '');
    if (!v){ node.style.display = 'none'; return null; }
    if (res && res.state === 'stale'){
      var d = hgBuildDistance(res.loaded, res.live);
      var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
      node.textContent = v + ' · STALE' + behind;
      node.className = 'verbadge verbadge-stale';
      node.title = (res.reason || 'stale') + ' — reloading…';
      return { stale: true };
    }
    node.textContent = v;
    node.className = 'verbadge';
    node.title = HG_BUILD.version + (HG_BUILD.pack ? (' · ' + HG_BUILD.pack) : '');
    return { stale: false };
  }catch(e){ return null; }
}

function hgBuildMaybeReload(res, storage, reloadFn){
  try{
    if (!res || res.state !== 'stale' || !res.live) return false;
    var store = storage;
    if (!store && G.sessionStorage) store = G.sessionStorage;
    if (!store || typeof store.getItem !== 'function') return false;
    var key = 'hg_build_reload_' + String(res.live);
    if (store.getItem(key)) return false;
    var reload = reloadFn;
    if (!reload && G.location && typeof G.location.reload === 'function') reload = G.location.reload.bind(G.location);
    if (typeof reload !== 'function') return false;
    store.setItem(key, '1');
    reload();
    return true;
  }catch(e){}
  return false;
}

function hgBuildApplyFreshness(res){
  try{
    if (!res) return res;
    hgRenderBuildChip(null, res);
    hgRenderVerBadge(res);
    G.HG_BUILD_FRESHNESS = res;
    hgBuildMaybeReload(res);
    return res;
  }catch(e){ return res; }
}

function hgBuildInit(){
  try{
    hgRenderBuildChip(null, null);
    hgRenderVerBadge(null);
    hgBuildFreshness().then(function(res){
      return hgBuildSwWaiting().then(function(waiting){
        if (waiting && res.state === 'fresh'){
          res = { state: 'stale', loaded: res.loaded, live: res.live, reason: 'new build downloaded' };
        }
        return hgBuildApplyFreshness(res);
      });
    });
    if (G.document && typeof G.document.addEventListener === 'function'){
      G.document.addEventListener('visibilitychange', function(){
        try{
          if (!G.document || G.document.visibilityState !== 'visible') return;
          hgBuildFreshness().then(hgBuildApplyFreshness);
        }catch(e){}
      });
    }
  }catch(e){}
}

G.HG_BUILD = HG_BUILD;
G.hgBuildLabel = hgBuildLabel;
G.hgBuildParseVersion = hgBuildParseVersion;
G.hgBuildCompare = hgBuildCompare;
G.hgBuildDistance = hgBuildDistance;
G.hgBuildChipState = hgBuildChipState;
G.hgBuildFreshness = hgBuildFreshness;
G.hgBuildSwWaiting = hgBuildSwWaiting;
G.hgRenderBuildChip = hgRenderBuildChip;
G.hgRenderVerBadge = hgRenderVerBadge;
G.hgBuildMaybeReload = hgBuildMaybeReload;
G.hgBuildApplyFreshness = hgBuildApplyFreshness;
G.hgBuildInit = hgBuildInit;

try{
  if (G.document){
    if (G.document.readyState === 'loading'){
      G.document.addEventListener('DOMContentLoaded', hgBuildInit);
    } else {
      hgBuildInit();
    }
  }
}catch(e){}

})();
