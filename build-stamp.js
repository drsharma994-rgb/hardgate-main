/* HARDGATE — build stamp. Single source of truth for "which version am I running?"
   Loaded FIRST so every later script can read G.HG_BUILD. */
(function(){
'use strict';
var G = (typeof window !== 'undefined') ? window : globalThis;

var HG_BUILD = {
  version: 'hg-v910',
  pack: 'THE GOLD TABS EVIDENCE CITATIONS NOW MATCH THE FILES THEY NAME. The gold sources justify live gates in prose - the 140-day swing replay measured the MP cohort at -0.209R, so a stamped-no-trade card can no longer lead - and that sentence IS the reason the rule exists. Nothing checked it. Sweeping every scripts/*.json citation in the gold sources and checking the numeric claims near it: NINE claims across five files named a file that does not contain them. NOT ONE WAS FABRICATED - every figure was exact - but each lived in the PRE-v700 twin of the file named, or in a generation of the bake since replaced. THAT IS WORSE THAN A TYPO, because of WHY those cohorts moved. goldswing bypass mints bySession n/a n=119 -0.544R: bucket GONE. goldswing MP cohort mpOnly n=100 -0.209R: now n=0. goldswing overall n=292 -0.158R: now n=244 +0.081R. omnigold1 stand-asides byFormation STOOD-ASIDE n=229: key GONE. EACH VANISHED COHORT IS THE PROOF THE FIX WORKED - the v700 session gate left no un-gated mints to measure, the NO_TRADE demote left no NO_TRADE card leading, the composed stop floor left no card to stand aside. Citing the current file for the old numbers pointed the reader at a file whose SILENCE on those cohorts was the result, and made it look like an error. AND ONE LIVE TABLE HAD DRIFTED. gold-formation HG_GOLD_SESSION_EVIDENCE carried a comment claiming its derivation reproduces every line exactly; it no longer did - baked from 8,155 rows against the 8,132 the file now holds. medCostR still matched on all five buckets, which is what said the METHOD was right and the data had moved under it. Re-derived and pinned: ASIA n=2320 +0.064, LONDON n=1430 -0.137, NY-OVERLAP n=2626 -0.081, NY-PM n=1044 +0.109, OFF n=712 -0.045. NO CONFIRMS VERDICT CHANGED - confirms is grossR >= 0 and all five signs are what they were across all three bakes. A SECOND COPY OF THAT TABLE, in the file header, had drifted furthest of all: it still carried the 7,270-row generation while the live array had already moved twice. Two copies of a measurement are two things to drift; both are now read back out of the cited file. OMNIGOLD provenance corrected too - window 2026-03-27..2026-09-10 and 54 kinds summing to n=7953, against the 2026-03-15..2026-08-29 and n=7270 it asserted (7270 is gold-setup-edge settledScan, a different file). A CLAIM OF MINE CORRECTED MID-PACK: I called the stale AUC user-facing; it is not. The rendered footnote reads fit.testAUC off the constant, which already held 0.5044, so what drifted was the prose beside the code and not the card. NOTHING WAS RE-BAKED: the replays cannot run here, the bar sources are refused at the gateway, so every corrected figure is read back out of a committed file. The superseded figures are RECORDED rather than deleted - a number that once justified a live table is worth recognising if it surfaces again - and a test asserts they appear only inside the note marking them superseded, never as current. THE GUARD: test-gold-evidence-citations.mjs pins twenty quoted figures to a file and a path, re-derives the session table from the bake on every run, checks the prose copy against the array the desk reads, and fails when any scripts/*.json a gold source names is missing from disk - which catches the wrong-case filename class outright. G1-G7, the gold stop floors and ceilings, the suppress, demote and prefer bars, the Wilson lower bound and the 80% accuracy floor are unchanged. Record only, nothing fetched or sent',
  built: '2026-09-20T00:00:00Z'
};

function hgBuildLabel(b){
  b = b || HG_BUILD;
  var v = String(b.version || '').replace(/^hg-/, '');
  var p = b.pack ? ' · ' + b.pack : '';
  return v ? v + p : 'unknown build';
}

function hgBuildParseVersion(text){
  try{
    if (typeof text !== 'string' || !text) return null;
    var m = text.match(/version\s*:\s*['"]([A-Za-z0-9._-]{1,64})['"]/);
    return m ? m[1] : null;
  }catch(e){ return null; }
}

function hgBuildCompare(loaded, live){
  if (!loaded || !live) return { state: 'unknown', reason: 'could not read one side' };
  if (String(loaded) === String(live)) return { state: 'fresh', reason: 'matches server' };
  return { state: 'stale', reason: 'server is on ' + live + ', this tab loaded ' + loaded };
}

function hgBuildDistance(loaded, live){
  try{
    var a = String(loaded || '').match(/(\d+)\s*$/);
    var b = String(live || '').match(/(\d+)\s*$/);
    if (!a || !b) return null;
    return (+b[1]) - (+a[1]);
  }catch(e){ return null; }
}

function hgBuildChipState(res, b){
  b = b || HG_BUILD;
  var label = hgBuildLabel(b);
  if (!res || res.state === 'pending') return { text: label, cls: 'ok', title: 'checking for newer build' };
  if (res.state === 'fresh') return { text: label, cls: 'ok', title: 'running current build (' + res.live + ')' };
  if (res.state === 'stale'){
    var d = hgBuildDistance(res.loaded, res.live);
    var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
    return { text: label + ' · STALE' + behind, cls: 'bad', title: res.reason + ' — hard-reload to pick it up' };
  }
  return { text: label + ' · ?', cls: 'warn', title: (res.reason || 'freshness unknown') + ' — offline or blocked' };
}

function hgBuildFreshness(fetchImpl){
  var f = (typeof fetchImpl === 'function') ? fetchImpl : (typeof G.fetch === 'function' ? G.fetch.bind(G) : null);
  var loaded = HG_BUILD.version;
  if (!f) return Promise.resolve({ state: 'unknown', loaded: loaded, live: null, reason: 'no fetch' });
  var url = './build-stamp.js?fresh=' + Date.now();
  return f(url, { cache: 'no-store' }).then(function(res){
    if (!res || !res.ok) throw new Error('http ' + (res && res.status));
    return res.text();
  }).then(function(text){
    var live = hgBuildParseVersion(text);
    var cmp = hgBuildCompare(loaded, live);
    return { state: cmp.state, loaded: loaded, live: live, reason: cmp.reason };
  }).catch(function(e){
    return { state: 'unknown', loaded: loaded, live: null, reason: (e && e.message) || 'network failed' };
  });
}

function hgBuildSwWaiting(){
  try{
    var sw = G.navigator && G.navigator.serviceWorker;
    if (!sw || !sw.controller) return Promise.resolve(false);
    return sw.getRegistration().then(function(reg){
      return !!(reg && reg.waiting);
    }).catch(function(){ return false; });
  }catch(e){ return Promise.resolve(false); }
}

function hgRenderBuildChip(el, res){
  try{
    var node = el || (G.document && G.document.getElementById('chipBuild'));
    if (!node) return null;
    var st = hgBuildChipState(res);
    node.textContent = '';
    var b = G.document.createElement('b');
    b.textContent = st.text;
    node.appendChild(b);
    node.title = st.title;
    node.className = 'statuschip hg-build-' + st.cls;
    return st;
  }catch(e){ return null; }
}

function hgRenderVerBadge(res){
  try{
    var node = G.document && G.document.getElementById('hgVerBadge');
    if (!node) return null;
    var v = String(HG_BUILD.version || '').replace(/^hg-/, '');
    if (!v){ node.style.display = 'none'; return null; }
    if (res && res.state === 'stale'){
      var d = hgBuildDistance(res.loaded, res.live);
      var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
      node.textContent = v + ' · STALE' + behind;
      node.className = 'verbadge verbadge-stale';
      node.title = (res.reason || 'stale') + ' — reloading…';
      return { stale: true };
    }
    node.textContent = v;
    node.className = 'verbadge';
    node.title = HG_BUILD.version + (HG_BUILD.pack ? (' · ' + HG_BUILD.pack) : '');
    return { stale: false };
  }catch(e){ return null; }
}

function hgBuildMaybeReload(res, storage, reloadFn){
  try{
    if (!res || res.state !== 'stale' || !res.live) return false;
    var store = storage;
    if (!store && G.sessionStorage) store = G.sessionStorage;
    if (!store || typeof store.getItem !== 'function') return false;
    var key = 'hg_build_reload_' + String(res.live);
    if (store.getItem(key)) return false;
    var reload = reloadFn;
    if (!reload && G.location && typeof G.location.reload === 'function') reload = G.location.reload.bind(G.location);
    if (typeof reload !== 'function') return false;
    store.setItem(key, '1');
    reload();
    return true;
  }catch(e){}
  return false;
}

function hgBuildApplyFreshness(res){
  try{
    if (!res) return res;
    hgRenderBuildChip(null, res);
    hgRenderVerBadge(res);
    G.HG_BUILD_FRESHNESS = res;
    hgBuildMaybeReload(res);
    return res;
  }catch(e){ return res; }
}

function hgBuildInit(){
  try{
    hgRenderBuildChip(null, null);
    hgRenderVerBadge(null);
    hgBuildFreshness().then(function(res){
      return hgBuildSwWaiting().then(function(waiting){
        if (waiting && res.state === 'fresh'){
          res = { state: 'stale', loaded: res.loaded, live: res.live, reason: 'new build downloaded' };
        }
        return hgBuildApplyFreshness(res);
      });
    });
    if (G.document && typeof G.document.addEventListener === 'function'){
      G.document.addEventListener('visibilitychange', function(){
        try{
          if (!G.document || G.document.visibilityState !== 'visible') return;
          hgBuildFreshness().then(hgBuildApplyFreshness);
        }catch(e){}
      });
    }
  }catch(e){}
}

G.HG_BUILD = HG_BUILD;
G.hgBuildLabel = hgBuildLabel;
G.hgBuildParseVersion = hgBuildParseVersion;
G.hgBuildCompare = hgBuildCompare;
G.hgBuildDistance = hgBuildDistance;
G.hgBuildChipState = hgBuildChipState;
G.hgBuildFreshness = hgBuildFreshness;
G.hgBuildSwWaiting = hgBuildSwWaiting;
G.hgRenderBuildChip = hgRenderBuildChip;
G.hgRenderVerBadge = hgRenderVerBadge;
G.hgBuildMaybeReload = hgBuildMaybeReload;
G.hgBuildApplyFreshness = hgBuildApplyFreshness;
G.hgBuildInit = hgBuildInit;

try{
  if (G.document){
    if (G.document.readyState === 'loading'){
      G.document.addEventListener('DOMContentLoaded', hgBuildInit);
    } else {
      hgBuildInit();
    }
  }
}catch(e){}

})();
