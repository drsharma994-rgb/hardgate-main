/* HARDGATE — build stamp. Single source of truth for "which version am I running?"
   Loaded FIRST so every later script can read G.HG_BUILD. */
(function(){
'use strict';
var G = (typeof window !== 'undefined') ? window : globalThis;

var HG_BUILD = {
  version: 'hg-v955',
  pack: 'THE GOLD CALENDAR MARK WAS WRITTEN BY EIGHT FILES AND READ BY NONE, AND THE FORWARD LEDGER DISCARDED IT AT THE DOOR. hg-v952 marked TAURIC rows, hg-v953 the GOLD SCALP and GOLD SWING mints, hg-v954 GOLD DIRECTION and GOLD ULTRA ticket rows. Every one of those packs justified MARKING rather than withholding on the same grounds: the rows need the mark so the population can be SEPARATED later. The separation was impossible. hgFwdNormalize builds an explicit fifteen-key record and goldShut was not among them, so every mark was dropped on the way into storage. hgFwdRecordScan had its own explicit field list and dropped it a second time. And GOLD SCALP and GOLD SWING REBUILD the row inside a map, dropping it a third time, so hg-v953 mint mark never left the candidate either. That is hg-v932 finding - the work was done and dropped on the way out - and this time in my own packs. The precedent sits three fields above the hole: mark was accepted-but-never-forwarded for exactly this reason, and the comment saying so is still there. THE FIELD NOW SURVIVES all three seams, and a READER exists, because a mark nothing reads is ornamental: hgFwdGoldCalendarSplit reports the live ledger split and hgFwdGoldCalendarHtml renders one line through hgGoldFwdNote, the one seam every gold desk already paints. The aggregate folds a tradeable and a weekend bucket beside gateClear and shown, for the same reason those fold - the live list is capped and pruned, so a split that lives only there is unanswerable past about four weeks. THREE STATES, NOT TWO, IS THE DESIGN POINT. Every record written before this carries no mark, and reading not-shut as tradeable would count the whole legacy ledger as gold-open - the same error one layer out, and exactly what made NEW GOLD record read positive at hg-v949. undefined means NOT RECORDED, is counted and named as its own bucket, and never folds into either side. The panel renders NOTHING until a record carries the mark, because a line reading zero weekend trades on a ledger that never recorded the field is the false reassurance this pack exists to remove. A HAZARD IN MY OWN RULE, FOUND BY MUTATION: the first cut wrote rec.goldShut === true, which turns a truthy non-boolean into FALSE - gold-OPEN - so this field own error would have arrived by coercion instead of by omission. The mirror, double-negation, reads the string no as shut. Only the two booleans are accepted now and everything else is the third state, proved on 1, 0, a string, an empty string and an object. A SECOND DEFECT IN THIS PACK OWN GUARD: the fixture mutated the rows hgFwdRecords hands back, and that function re-parses storage on every call, so the settle never stuck - two assertions passed on null === null. The settled state is written back under the module own key and the settled COUNT is asserted first. AND A THIRD: the aggregate section grepped hg-forward.js for the two fold lines, the brittle shape corrected four times this session and one that cannot see whether a branch is reachable. hgFwdFold is exported and CALLED, with the three buckets required to partition the rows. Nothing is gated on any of it: a weekend-formed row is still recorded, still a ticket, still resolved, and its levels are untouched. No gate, threshold or verdict moves on any desk, and no setup leaves any board. Tests: tests/test-gold-forward-calendar.mjs, 68 assertions, 19 of 19 behavioural mutations caught across hg-forward.js, goldscalp.js, goldswing.js and gold-forward-read.js with the baseline verified green before every pass',
  built: '2026-09-24T00:00:00Z'
};

function hgBuildLabel(b){
  b = b || HG_BUILD;
  var v = String(b.version || '').replace(/^hg-/, '');
  var p = b.pack ? ' · ' + b.pack : '';
  return v ? v + p : 'unknown build';
}

function hgBuildParseVersion(text){
  try{
    if (typeof text !== 'string' || !text) return null;
    var m = text.match(/version\s*:\s*['"]([A-Za-z0-9._-]{1,64})['"]/);
    return m ? m[1] : null;
  }catch(e){ return null; }
}

function hgBuildCompare(loaded, live){
  if (!loaded || !live) return { state: 'unknown', reason: 'could not read one side' };
  if (String(loaded) === String(live)) return { state: 'fresh', reason: 'matches server' };
  return { state: 'stale', reason: 'server is on ' + live + ', this tab loaded ' + loaded };
}

function hgBuildDistance(loaded, live){
  try{
    var a = String(loaded || '').match(/(\d+)\s*$/);
    var b = String(live || '').match(/(\d+)\s*$/);
    if (!a || !b) return null;
    return (+b[1]) - (+a[1]);
  }catch(e){ return null; }
}

function hgBuildChipState(res, b){
  b = b || HG_BUILD;
  var label = hgBuildLabel(b);
  if (!res || res.state === 'pending') return { text: label, cls: 'ok', title: 'checking for newer build' };
  if (res.state === 'fresh') return { text: label, cls: 'ok', title: 'running current build (' + res.live + ')' };
  if (res.state === 'stale'){
    var d = hgBuildDistance(res.loaded, res.live);
    var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
    return { text: label + ' · STALE' + behind, cls: 'bad', title: res.reason + ' — hard-reload to pick it up' };
  }
  return { text: label + ' · ?', cls: 'warn', title: (res.reason || 'freshness unknown') + ' — offline or blocked' };
}

function hgBuildFreshness(fetchImpl){
  var f = (typeof fetchImpl === 'function') ? fetchImpl : (typeof G.fetch === 'function' ? G.fetch.bind(G) : null);
  var loaded = HG_BUILD.version;
  if (!f) return Promise.resolve({ state: 'unknown', loaded: loaded, live: null, reason: 'no fetch' });
  var url = './build-stamp.js?fresh=' + Date.now();
  return f(url, { cache: 'no-store' }).then(function(res){
    if (!res || !res.ok) throw new Error('http ' + (res && res.status));
    return res.text();
  }).then(function(text){
    var live = hgBuildParseVersion(text);
    var cmp = hgBuildCompare(loaded, live);
    return { state: cmp.state, loaded: loaded, live: live, reason: cmp.reason };
  }).catch(function(e){
    return { state: 'unknown', loaded: loaded, live: null, reason: (e && e.message) || 'network failed' };
  });
}

function hgBuildSwWaiting(){
  try{
    var sw = G.navigator && G.navigator.serviceWorker;
    if (!sw || !sw.controller) return Promise.resolve(false);
    return sw.getRegistration().then(function(reg){
      return !!(reg && reg.waiting);
    }).catch(function(){ return false; });
  }catch(e){ return Promise.resolve(false); }
}

function hgRenderBuildChip(el, res){
  try{
    var node = el || (G.document && G.document.getElementById('chipBuild'));
    if (!node) return null;
    var st = hgBuildChipState(res);
    node.textContent = '';
    var b = G.document.createElement('b');
    b.textContent = st.text;
    node.appendChild(b);
    node.title = st.title;
    node.className = 'statuschip hg-build-' + st.cls;
    return st;
  }catch(e){ return null; }
}

function hgRenderVerBadge(res){
  try{
    var node = G.document && G.document.getElementById('hgVerBadge');
    if (!node) return null;
    var v = String(HG_BUILD.version || '').replace(/^hg-/, '');
    if (!v){ node.style.display = 'none'; return null; }
    if (res && res.state === 'stale'){
      var d = hgBuildDistance(res.loaded, res.live);
      var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
      node.textContent = v + ' · STALE' + behind;
      node.className = 'verbadge verbadge-stale';
      node.title = (res.reason || 'stale') + ' — reloading…';
      return { stale: true };
    }
    node.textContent = v;
    node.className = 'verbadge';
    node.title = HG_BUILD.version + (HG_BUILD.pack ? (' · ' + HG_BUILD.pack) : '');
    return { stale: false };
  }catch(e){ return null; }
}

function hgBuildMaybeReload(res, storage, reloadFn){
  try{
    if (!res || res.state !== 'stale' || !res.live) return false;
    var store = storage;
    if (!store && G.sessionStorage) store = G.sessionStorage;
    if (!store || typeof store.getItem !== 'function') return false;
    var key = 'hg_build_reload_' + String(res.live);
    if (store.getItem(key)) return false;
    var reload = reloadFn;
    if (!reload && G.location && typeof G.location.reload === 'function') reload = G.location.reload.bind(G.location);
    if (typeof reload !== 'function') return false;
    store.setItem(key, '1');
    reload();
    return true;
  }catch(e){}
  return false;
}

function hgBuildApplyFreshness(res){
  try{
    if (!res) return res;
    hgRenderBuildChip(null, res);
    hgRenderVerBadge(res);
    G.HG_BUILD_FRESHNESS = res;
    hgBuildMaybeReload(res);
    return res;
  }catch(e){ return res; }
}

function hgBuildInit(){
  try{
    hgRenderBuildChip(null, null);
    hgRenderVerBadge(null);
    hgBuildFreshness().then(function(res){
      return hgBuildSwWaiting().then(function(waiting){
        if (waiting && res.state === 'fresh'){
          res = { state: 'stale', loaded: res.loaded, live: res.live, reason: 'new build downloaded' };
        }
        return hgBuildApplyFreshness(res);
      });
    });
    if (G.document && typeof G.document.addEventListener === 'function'){
      G.document.addEventListener('visibilitychange', function(){
        try{
          if (!G.document || G.document.visibilityState !== 'visible') return;
          hgBuildFreshness().then(hgBuildApplyFreshness);
        }catch(e){}
      });
    }
  }catch(e){}
}

G.HG_BUILD = HG_BUILD;
G.hgBuildLabel = hgBuildLabel;
G.hgBuildParseVersion = hgBuildParseVersion;
G.hgBuildCompare = hgBuildCompare;
G.hgBuildDistance = hgBuildDistance;
G.hgBuildChipState = hgBuildChipState;
G.hgBuildFreshness = hgBuildFreshness;
G.hgBuildSwWaiting = hgBuildSwWaiting;
G.hgRenderBuildChip = hgRenderBuildChip;
G.hgRenderVerBadge = hgRenderVerBadge;
G.hgBuildMaybeReload = hgBuildMaybeReload;
G.hgBuildApplyFreshness = hgBuildApplyFreshness;
G.hgBuildInit = hgBuildInit;

try{
  if (G.document){
    if (G.document.readyState === 'loading'){
      G.document.addEventListener('DOMContentLoaded', hgBuildInit);
    } else {
      hgBuildInit();
    }
  }
}catch(e){}

})();
