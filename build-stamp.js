/* HARDGATE — build stamp. Single source of truth for "which version am I running?"
   Loaded FIRST so every later script can read G.HG_BUILD. */
(function(){
'use strict';
var G = (typeof window !== 'undefined') ? window : globalThis;

var HG_BUILD = {
  version: 'hg-v944',
  pack: 'THE LOUDEST GATE ON GOLD SCALP IS NOT THE ONE EMPTYING THE BOARD, AND THE NUMBER THAT SHOWS IT IS THE ONE NOBODY HAD. Asked a third time for better gold setup formation: hg-v942 added the six measured-profitable mechanics these tabs could not form, hg-v943 stopped promoting on a contradicted record, and this looks at the rule that decides WHO MAY LEAD AT ALL. goldind demotes every setup formed outside an ICT killzone, and a demoted row can never be MOST PROBABLE. On the committed walk that stamp lands on 1,156 of 2,605 formed setups - 44.4% - and ZERO of 111 MOST PROBABLE rows were off-session. Read alone those two figures say session filtering decides this board, and the obvious move is to relax it. THE SOLE-BLOCKER COUNT SAYS OTHERWISE: OFF-SESSION is the ONLY demote on SIX rows - 0.5% of the rows carrying it and 0.2% of the walk. Relaxing it entirely would return six setups, not 1,156. What is actually demoting them is CONF NO TRADE 1,504, EDGE DEMOTE 784, ASIA SESSION 567, MTF BIAS 386 and S56 BALANCE 223. This is the hg-v929 finding in a second place - a share is not a cost, and the loud gate is not the binding one - and it is hg-v184 crypto sole-blocker discipline applied to gold. I NEARLY SHIPPED THE OVERSTATEMENT. The first cut of this pack led with the 44.4% share and the zero-leaders figure, which together invite exactly the wrong inference. The sole-blocker count was computed only because pulling the lever on the real mint moved NOTHING, and that had to be explained rather than ignored. SEPARATELY, TIME OF DAY SEPARATES NOTHING. On the bar every gold verdict here must clear - four DISJOINT windows agreeing on win rate AND gross AND net, at BOTH fill bounds - not one killzone split carries a verdict. The cohort the rule most FAVOURS, the London and NY overlap at maximum weight, points the WRONG way at minus 0.044 and minus 0.031 net, agreeing in one window of four; the cohort it demotes reads mildly POSITIVE at plus 0.021 and plus 0.032, two of four. Neither is unanimous. SO NOTHING CHANGES, FOR TWO INDEPENDENT REASONS: the evidence clears the bar in neither direction, and the rule is not what withholds setups anyway. Removing it would be loosening on non-unanimous evidence, which hg-v920 refused three times. A lever exists for the desk owner, hgGoldSetOffSessionDemote(false), and it DEFAULTS TO ON - unchanged. Measured on the real mint over 120 scans: 173 candidates, 164 demoted, 9 lead-eligible, IDENTICAL with the lever either way, which is what a six-row sole-blocker count predicts. Two other things this corrected as it went. The killzone weight does NOT feed the grade on the path the walk exercises - the recorded grade is the tally alone at 100.0% of 2,283 rows - so a reader of the score-plus-weight line in goldind would believe a lever that is not there. And GOLD SWING is untouched on purpose: it states that killzones are deliberately not a swing gate, so the desk difference is a documented choice and not drift. GENERATED, NOT TRANSCRIBED: scripts/session-separation.mjs writes every figure from the committed replay, the demote-stamp set is ENUMERATED so the sole count can be audited, a missing marker is fatal, and the tree round-trips to zero drift. The panel renders NOTHING without the literal rather than a zero-filled table, and it BRANCHES on the verdict so a future bake that finds one is announced instead of hidden behind today answer. Tests: tests/test-session-separation.mjs (75 assertions), 17 of 17 behavioural mutations caught across two files, baseline verified green first. Suite: 596 files, 0 failed',
  built: '2026-09-23T00:00:00Z'
};

function hgBuildLabel(b){
  b = b || HG_BUILD;
  var v = String(b.version || '').replace(/^hg-/, '');
  var p = b.pack ? ' · ' + b.pack : '';
  return v ? v + p : 'unknown build';
}

function hgBuildParseVersion(text){
  try{
    if (typeof text !== 'string' || !text) return null;
    var m = text.match(/version\s*:\s*['"]([A-Za-z0-9._-]{1,64})['"]/);
    return m ? m[1] : null;
  }catch(e){ return null; }
}

function hgBuildCompare(loaded, live){
  if (!loaded || !live) return { state: 'unknown', reason: 'could not read one side' };
  if (String(loaded) === String(live)) return { state: 'fresh', reason: 'matches server' };
  return { state: 'stale', reason: 'server is on ' + live + ', this tab loaded ' + loaded };
}

function hgBuildDistance(loaded, live){
  try{
    var a = String(loaded || '').match(/(\d+)\s*$/);
    var b = String(live || '').match(/(\d+)\s*$/);
    if (!a || !b) return null;
    return (+b[1]) - (+a[1]);
  }catch(e){ return null; }
}

function hgBuildChipState(res, b){
  b = b || HG_BUILD;
  var label = hgBuildLabel(b);
  if (!res || res.state === 'pending') return { text: label, cls: 'ok', title: 'checking for newer build' };
  if (res.state === 'fresh') return { text: label, cls: 'ok', title: 'running current build (' + res.live + ')' };
  if (res.state === 'stale'){
    var d = hgBuildDistance(res.loaded, res.live);
    var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
    return { text: label + ' · STALE' + behind, cls: 'bad', title: res.reason + ' — hard-reload to pick it up' };
  }
  return { text: label + ' · ?', cls: 'warn', title: (res.reason || 'freshness unknown') + ' — offline or blocked' };
}

function hgBuildFreshness(fetchImpl){
  var f = (typeof fetchImpl === 'function') ? fetchImpl : (typeof G.fetch === 'function' ? G.fetch.bind(G) : null);
  var loaded = HG_BUILD.version;
  if (!f) return Promise.resolve({ state: 'unknown', loaded: loaded, live: null, reason: 'no fetch' });
  var url = './build-stamp.js?fresh=' + Date.now();
  return f(url, { cache: 'no-store' }).then(function(res){
    if (!res || !res.ok) throw new Error('http ' + (res && res.status));
    return res.text();
  }).then(function(text){
    var live = hgBuildParseVersion(text);
    var cmp = hgBuildCompare(loaded, live);
    return { state: cmp.state, loaded: loaded, live: live, reason: cmp.reason };
  }).catch(function(e){
    return { state: 'unknown', loaded: loaded, live: null, reason: (e && e.message) || 'network failed' };
  });
}

function hgBuildSwWaiting(){
  try{
    var sw = G.navigator && G.navigator.serviceWorker;
    if (!sw || !sw.controller) return Promise.resolve(false);
    return sw.getRegistration().then(function(reg){
      return !!(reg && reg.waiting);
    }).catch(function(){ return false; });
  }catch(e){ return Promise.resolve(false); }
}

function hgRenderBuildChip(el, res){
  try{
    var node = el || (G.document && G.document.getElementById('chipBuild'));
    if (!node) return null;
    var st = hgBuildChipState(res);
    node.textContent = '';
    var b = G.document.createElement('b');
    b.textContent = st.text;
    node.appendChild(b);
    node.title = st.title;
    node.className = 'statuschip hg-build-' + st.cls;
    return st;
  }catch(e){ return null; }
}

function hgRenderVerBadge(res){
  try{
    var node = G.document && G.document.getElementById('hgVerBadge');
    if (!node) return null;
    var v = String(HG_BUILD.version || '').replace(/^hg-/, '');
    if (!v){ node.style.display = 'none'; return null; }
    if (res && res.state === 'stale'){
      var d = hgBuildDistance(res.loaded, res.live);
      var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
      node.textContent = v + ' · STALE' + behind;
      node.className = 'verbadge verbadge-stale';
      node.title = (res.reason || 'stale') + ' — reloading…';
      return { stale: true };
    }
    node.textContent = v;
    node.className = 'verbadge';
    node.title = HG_BUILD.version + (HG_BUILD.pack ? (' · ' + HG_BUILD.pack) : '');
    return { stale: false };
  }catch(e){ return null; }
}

function hgBuildMaybeReload(res, storage, reloadFn){
  try{
    if (!res || res.state !== 'stale' || !res.live) return false;
    var store = storage;
    if (!store && G.sessionStorage) store = G.sessionStorage;
    if (!store || typeof store.getItem !== 'function') return false;
    var key = 'hg_build_reload_' + String(res.live);
    if (store.getItem(key)) return false;
    var reload = reloadFn;
    if (!reload && G.location && typeof G.location.reload === 'function') reload = G.location.reload.bind(G.location);
    if (typeof reload !== 'function') return false;
    store.setItem(key, '1');
    reload();
    return true;
  }catch(e){}
  return false;
}

function hgBuildApplyFreshness(res){
  try{
    if (!res) return res;
    hgRenderBuildChip(null, res);
    hgRenderVerBadge(res);
    G.HG_BUILD_FRESHNESS = res;
    hgBuildMaybeReload(res);
    return res;
  }catch(e){ return res; }
}

function hgBuildInit(){
  try{
    hgRenderBuildChip(null, null);
    hgRenderVerBadge(null);
    hgBuildFreshness().then(function(res){
      return hgBuildSwWaiting().then(function(waiting){
        if (waiting && res.state === 'fresh'){
          res = { state: 'stale', loaded: res.loaded, live: res.live, reason: 'new build downloaded' };
        }
        return hgBuildApplyFreshness(res);
      });
    });
    if (G.document && typeof G.document.addEventListener === 'function'){
      G.document.addEventListener('visibilitychange', function(){
        try{
          if (!G.document || G.document.visibilityState !== 'visible') return;
          hgBuildFreshness().then(hgBuildApplyFreshness);
        }catch(e){}
      });
    }
  }catch(e){}
}

G.HG_BUILD = HG_BUILD;
G.hgBuildLabel = hgBuildLabel;
G.hgBuildParseVersion = hgBuildParseVersion;
G.hgBuildCompare = hgBuildCompare;
G.hgBuildDistance = hgBuildDistance;
G.hgBuildChipState = hgBuildChipState;
G.hgBuildFreshness = hgBuildFreshness;
G.hgBuildSwWaiting = hgBuildSwWaiting;
G.hgRenderBuildChip = hgRenderBuildChip;
G.hgRenderVerBadge = hgRenderVerBadge;
G.hgBuildMaybeReload = hgBuildMaybeReload;
G.hgBuildApplyFreshness = hgBuildApplyFreshness;
G.hgBuildInit = hgBuildInit;

try{
  if (G.document){
    if (G.document.readyState === 'loading'){
      G.document.addEventListener('DOMContentLoaded', hgBuildInit);
    } else {
      hgBuildInit();
    }
  }
}catch(e){}

})();
