/* HARDGATE — build stamp. Single source of truth for "which version am I running?"
   Loaded FIRST so every later script can read G.HG_BUILD. */
(function(){
'use strict';
var G = (typeof window !== 'undefined') ? window : globalThis;

var HG_BUILD = {
  version: 'hg-v956',
  pack: 'WHAT THE GOLD WEEKEND ACTUALLY COSTS THE EDGE TABLE, AND THE GATE THAT WAS ALREADY PAYING FOR IT - INCLUDING TWO CORRECTIONS TO MY OWN hg-v953. FIRST, THE WRONG FILE. scripts/ holds THREE goldscalp artifacts and they are not interchangeable. The edge table live column is computed by edge-live-population.mjs, which reads backtest-goldscalp-results-FLOOR.json at 2,605 trades, and AGENTS.md HG_GOLD_EDGE_WALK names that same span. hg-v953 quoted the 2,575-trade neighbour and called it what HG_GOLD_SETUP_EDGE is measured on. On the right file the weekend share is 27.2% rather than 26.4%, and the SET OF VERDICTS THAT WOULD MOVE IS DIFFERENT - asian and ribbon in, hvn and p8range out - so the file mattered, and the correction is an import rather than a sentence. SECOND, THE WRONG BASIS. The headline n, gross and net on every row are whole-book and contaminated; the live figures hg-v916 added and hg-v928 acted on are not. On the live basis it is SEVEN weekend rows in 1,205, which is 0.6%, and there is nothing to scope. THE MECHANISM, MEASURED RATHER THAN ASSUMED, and it is the finding. A weekend PAXG bar is a thin crypto proxy with almost no range, so ATR collapses, so the stop is narrow - and the hg-v912 cost reject removes narrow stops. Median stop width 0.038% on weekend rows against 0.266% on open ones, a factor of seven, against a 0.16% bar; 98.4% of weekend rows fail that gate against 6.4% of open rows. That gate has been paying for the calendar since it shipped and nobody had established it in either direction. NOTHING MOVES, for two independent reasons. Every one of the seven moving rows is THIN under the desk own bar - four disjoint windows at both fill bounds agreeing on win AND gross AND net, imported from factor-separation and demote-separation rather than rebuilt - and the pooled read is thin too, precisely because the cost gate already removed the cohort. The live basis has nothing left to correct. hg-v920 refused three loosenings on this evidence, hg-v944 a fourth, hg-v945 reported five leans without acting on one. A contaminated measurement is not a measurement of the opposite; it is a measurement nobody has made. AND THE FOURTH SEAM. hg-v955 found goldShut dropped by hgFwdNormalize, by hgFwdRecordScan and by the desks own map. The REPLAY ROW BUILDERS dropped it too - the place hg-v953 actually named as its reason for marking - so seven scripts read these artifacts and exactly one carried a calendar of its own. Both walks now carry the mark from the candidate onto the trade at EVERY build site, the live book and the EDGE-suppressed shadow book alike, and emit it from settleRecord, on the same three-state rule hg-v955 established: only the two booleans, everything else is NOT RECORDED. A single-site check passed while the other site silently lost the mark, and mutation caught exactly that. HONEST LIMIT, STATED IN THE GUARD: the committed artifacts carry no marks yet because a re-bake needs bars this environment cannot fetch, so every figure here is derived from tISO through the repo own DST-aware calendar, which the artifact does carry. The script CALLS that calendar and the guard asserts it holds no calendar arithmetic of its own. One assertion in this guard is deliberately textual and says so: whether a number was imported or retyped is a property of the source, and no runtime comparison can tell them apart while the two agree, which is exactly when the drift starts. No gate, threshold or verdict moves on any desk, and no setup leaves any board. Tests: tests/test-gold-edge-tradeable.mjs, 68 assertions, 17 of 17 behavioural mutations caught across scripts/gold-edge-tradeable.mjs, scripts/backtest-goldscalp.mjs and scripts/backtest-goldswing.mjs with the baseline verified green before every pass. Re-derive: node scripts/gold-edge-tradeable.mjs',
  built: '2026-09-24T00:00:00Z'
};

function hgBuildLabel(b){
  b = b || HG_BUILD;
  var v = String(b.version || '').replace(/^hg-/, '');
  var p = b.pack ? ' · ' + b.pack : '';
  return v ? v + p : 'unknown build';
}

function hgBuildParseVersion(text){
  try{
    if (typeof text !== 'string' || !text) return null;
    var m = text.match(/version\s*:\s*['"]([A-Za-z0-9._-]{1,64})['"]/);
    return m ? m[1] : null;
  }catch(e){ return null; }
}

function hgBuildCompare(loaded, live){
  if (!loaded || !live) return { state: 'unknown', reason: 'could not read one side' };
  if (String(loaded) === String(live)) return { state: 'fresh', reason: 'matches server' };
  return { state: 'stale', reason: 'server is on ' + live + ', this tab loaded ' + loaded };
}

function hgBuildDistance(loaded, live){
  try{
    var a = String(loaded || '').match(/(\d+)\s*$/);
    var b = String(live || '').match(/(\d+)\s*$/);
    if (!a || !b) return null;
    return (+b[1]) - (+a[1]);
  }catch(e){ return null; }
}

function hgBuildChipState(res, b){
  b = b || HG_BUILD;
  var label = hgBuildLabel(b);
  if (!res || res.state === 'pending') return { text: label, cls: 'ok', title: 'checking for newer build' };
  if (res.state === 'fresh') return { text: label, cls: 'ok', title: 'running current build (' + res.live + ')' };
  if (res.state === 'stale'){
    var d = hgBuildDistance(res.loaded, res.live);
    var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
    return { text: label + ' · STALE' + behind, cls: 'bad', title: res.reason + ' — hard-reload to pick it up' };
  }
  return { text: label + ' · ?', cls: 'warn', title: (res.reason || 'freshness unknown') + ' — offline or blocked' };
}

function hgBuildFreshness(fetchImpl){
  var f = (typeof fetchImpl === 'function') ? fetchImpl : (typeof G.fetch === 'function' ? G.fetch.bind(G) : null);
  var loaded = HG_BUILD.version;
  if (!f) return Promise.resolve({ state: 'unknown', loaded: loaded, live: null, reason: 'no fetch' });
  var url = './build-stamp.js?fresh=' + Date.now();
  return f(url, { cache: 'no-store' }).then(function(res){
    if (!res || !res.ok) throw new Error('http ' + (res && res.status));
    return res.text();
  }).then(function(text){
    var live = hgBuildParseVersion(text);
    var cmp = hgBuildCompare(loaded, live);
    return { state: cmp.state, loaded: loaded, live: live, reason: cmp.reason };
  }).catch(function(e){
    return { state: 'unknown', loaded: loaded, live: null, reason: (e && e.message) || 'network failed' };
  });
}

function hgBuildSwWaiting(){
  try{
    var sw = G.navigator && G.navigator.serviceWorker;
    if (!sw || !sw.controller) return Promise.resolve(false);
    return sw.getRegistration().then(function(reg){
      return !!(reg && reg.waiting);
    }).catch(function(){ return false; });
  }catch(e){ return Promise.resolve(false); }
}

function hgRenderBuildChip(el, res){
  try{
    var node = el || (G.document && G.document.getElementById('chipBuild'));
    if (!node) return null;
    var st = hgBuildChipState(res);
    node.textContent = '';
    var b = G.document.createElement('b');
    b.textContent = st.text;
    node.appendChild(b);
    node.title = st.title;
    node.className = 'statuschip hg-build-' + st.cls;
    return st;
  }catch(e){ return null; }
}

function hgRenderVerBadge(res){
  try{
    var node = G.document && G.document.getElementById('hgVerBadge');
    if (!node) return null;
    var v = String(HG_BUILD.version || '').replace(/^hg-/, '');
    if (!v){ node.style.display = 'none'; return null; }
    if (res && res.state === 'stale'){
      var d = hgBuildDistance(res.loaded, res.live);
      var behind = (d != null && d > 0) ? ' (' + d + ' behind)' : '';
      node.textContent = v + ' · STALE' + behind;
      node.className = 'verbadge verbadge-stale';
      node.title = (res.reason || 'stale') + ' — reloading…';
      return { stale: true };
    }
    node.textContent = v;
    node.className = 'verbadge';
    node.title = HG_BUILD.version + (HG_BUILD.pack ? (' · ' + HG_BUILD.pack) : '');
    return { stale: false };
  }catch(e){ return null; }
}

function hgBuildMaybeReload(res, storage, reloadFn){
  try{
    if (!res || res.state !== 'stale' || !res.live) return false;
    var store = storage;
    if (!store && G.sessionStorage) store = G.sessionStorage;
    if (!store || typeof store.getItem !== 'function') return false;
    var key = 'hg_build_reload_' + String(res.live);
    if (store.getItem(key)) return false;
    var reload = reloadFn;
    if (!reload && G.location && typeof G.location.reload === 'function') reload = G.location.reload.bind(G.location);
    if (typeof reload !== 'function') return false;
    store.setItem(key, '1');
    reload();
    return true;
  }catch(e){}
  return false;
}

function hgBuildApplyFreshness(res){
  try{
    if (!res) return res;
    hgRenderBuildChip(null, res);
    hgRenderVerBadge(res);
    G.HG_BUILD_FRESHNESS = res;
    hgBuildMaybeReload(res);
    return res;
  }catch(e){ return res; }
}

function hgBuildInit(){
  try{
    hgRenderBuildChip(null, null);
    hgRenderVerBadge(null);
    hgBuildFreshness().then(function(res){
      return hgBuildSwWaiting().then(function(waiting){
        if (waiting && res.state === 'fresh'){
          res = { state: 'stale', loaded: res.loaded, live: res.live, reason: 'new build downloaded' };
        }
        return hgBuildApplyFreshness(res);
      });
    });
    if (G.document && typeof G.document.addEventListener === 'function'){
      G.document.addEventListener('visibilitychange', function(){
        try{
          if (!G.document || G.document.visibilityState !== 'visible') return;
          hgBuildFreshness().then(hgBuildApplyFreshness);
        }catch(e){}
      });
    }
  }catch(e){}
}

G.HG_BUILD = HG_BUILD;
G.hgBuildLabel = hgBuildLabel;
G.hgBuildParseVersion = hgBuildParseVersion;
G.hgBuildCompare = hgBuildCompare;
G.hgBuildDistance = hgBuildDistance;
G.hgBuildChipState = hgBuildChipState;
G.hgBuildFreshness = hgBuildFreshness;
G.hgBuildSwWaiting = hgBuildSwWaiting;
G.hgRenderBuildChip = hgRenderBuildChip;
G.hgRenderVerBadge = hgRenderVerBadge;
G.hgBuildMaybeReload = hgBuildMaybeReload;
G.hgBuildApplyFreshness = hgBuildApplyFreshness;
G.hgBuildInit = hgBuildInit;

try{
  if (G.document){
    if (G.document.readyState === 'loading'){
      G.document.addEventListener('DOMContentLoaded', hgBuildInit);
    } else {
      hgBuildInit();
    }
  }
}catch(e){}

})();
